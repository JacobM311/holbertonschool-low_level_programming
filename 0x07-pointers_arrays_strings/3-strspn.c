#include "main.h"

/**
 * int_strspn-yes
 * @s: yes
 * @accept: yes
 */

unsigned int _strspn(char *s, char *accept)
{
	int count = 0;

	while (accept[i])
	{
		j = 0;
		while (s[j] != 32)
		{
			if (accept[i] = s[j])
			{
				count++;
			}
			j++;
		}
		i++;
	}
return (count);
}
